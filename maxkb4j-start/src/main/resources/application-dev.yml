spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/MaxKB4j
    username: tarzan_postgres
    password: ycn4NRhjN2
    driver-class-name: org.postgresql.Driver
  data:
    mongodb:
      uri: mongodb://tarzan_mongo:ycn4NRhjN2@localhost:27017/MaxKB4j?authSource=admin

# 文档目录增量监控配置
maxkb4j:
  document-watch:
    enabled: false          # 设为 true 开启监控
    interval-seconds: 60   # 扫描间隔（秒），默认60秒
    directories:
      - path: E:/docs/watch  # 要监控的本地目录
        knowledge-id: 81d794181abd5bb69e1ba89194a6f85f  # 在系统界面复制知识库ID
        name: 本地文档同步
